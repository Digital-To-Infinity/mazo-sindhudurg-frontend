# Backend API base URL. Use same-origin "/api" so requests go through the
# Next.js reverse proxy (see next.config.ts rewrites) and avoid CORS issues.
NEXT_PUBLIC_API_URL=/api

# Canonical site URL used in metadata, robots.txt, and sitemap.xml
NEXT_PUBLIC_SITE_URL=https://mazosindhudurg.com
